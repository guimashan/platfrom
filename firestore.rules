rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 預設規則：所有讀寫都需要透過 Cloud Functions
    // 前端不直接訪問 Firestore
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
